# After deploying the Cloudflare Worker, replace the placeholder below with your actual worker URL.
# Run: cd cloudflare && npx wrangler deploy
# Then copy the URL shown (e.g. https://peach-api-proxy.username.workers.dev)
VITE_API_BASE=https://peach-api-proxy.peachapi-proxy.workers.dev/
